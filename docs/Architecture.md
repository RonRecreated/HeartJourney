1. Heart Journey measures situations in order to provide meaningful guidance, not to assign worth or value to a person

- Feature-first organization
- Separation of domain models and feature view models
- Sanity as the content source
- Blazor as the experience layer
- UI-driven GROQ queries
- Reusable ministry content

Sanity publishing has two layers:
1. Sanity document publish makes the document available in the dataset.
2. The custom Published field controls whether Heart Journey displays the content in the app.

Both must be true for content to appear in the Blazor PWA.

Sanity = content CMS
Database = user data
Auth = identity/sign-in
Blazor = experience/UI